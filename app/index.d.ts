interface String {
    phoneFormat(length : number) : string;
    capitalize(): string;
    firstLetter(): string;
    restLetters(int:number): string;
    plural(revert?:any): string;
    allTrim(): string;
}
